package rules.smith;

import com.mindsmiths.ruleEngine.model.Initialize;
import com.mindsmiths.ruleEngine.util.Log;

rule "get signal"
    when
        initAgent: Initialize(agent: agent) from entry-point "new-pair"
    then
        agent.sendData();
        Log.info(+ agent);
        delete(initAgent);
end