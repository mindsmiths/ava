package rules.Ava;

import com.mindsmiths.ruleEngine.model.Initialize;
import com.mindsmiths.ruleEngine.util.Log;
import agents.Ava;

rule "get signal"
    when
        Initialize() from entry-point "new-pair"
        agent: Ava()
    then
        Log.info(agent);
end